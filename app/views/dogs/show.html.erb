<h2>Name: <%= @dog.name %></h2>
<h4>Breed: <%= @dog.breed %></h4>
<h4>Age: <%= @dog.age %></h4>
<h3>Owners</h3>
<ul>
    <% @dog.employees.each do |e| %>
        <li><%= link_to e.first_name + " " + e.last_name, employee_path(e) %></li>
    <% end %>
</ul>
<button><%= link_to "Back", dogs_path %></button>